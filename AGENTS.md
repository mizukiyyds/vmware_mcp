# AGENTS.md

## 项目文件组织

项目文件必须分好类。文件夹中可以继续建文件夹，按照用途等进行分类。基本分类如下：

- 代码 `code/`
- 图片 `picture/`
- 结论 `conclusion/`
- 数据 `data/`
- 文档 `document/`
- 参考资料 `reference/`
- 卡点 `blocker/`
- 其他类型按同样思路建立对应的文件夹

当觉得文件混乱、有必要重新分类时，可以询问用户是否对项目进行整理。

## 需求澄清

当用户的描述比较简洁或者描述不清晰可能有多种含义时，可以进行多次询问，确认意图后再动手。

当任务有多种解决方法时，给出各种解决方法以及它们的优缺点，询问用户选择方案。

## 环境确认

首先搞清楚操作系统是 Windows 还是 Linux、Mac 等，以及终端类型（Git Bash、PowerShell、cmd 等），据此选择兼容的命令和路径写法。

编写文档、代码时，需要注意用户的系统环境默认编码（如 Windows 中文环境常见 GBK），以防止在编辑器以及终端中出现乱码。必要时明确指定文件编码，并在输出到终端时考虑编码兼容性。

本项目 code/ 下的 Python 源文件统一保存为 GBK（无 BOM），每个文件第一行必须是 `# coding: gbk`。不要存成 UTF-8。写入后用 decode("gbk") 检查。Cursor 的 `.vscode/settings.json` 已将 Python 文件编码设为 GBK。

当需要执行代码，或者用到某些库时，首先用类似 `python --version`、`pip list` 等命令搞清楚环境，确认版本和已安装的库，再决定实现方案。

## 任务指导

优先考虑使用电脑已经安装的库或者查找开源项目进行数据解析，而不是自己编写脚本去解析。常用库没有安装时，优先向用户询问是否要安装，确实没有合适的库时才自行实现。遇到少见的文件类型需要解析数据时，也一样。

删除文件时不要真正删除，而是移动到 `backup/` 目录下，以便之后恢复。

当遇到卡点时（遇到困难或者报错导致停滞），在解决之后写一份给 AI Agent 阅读的说明，保存到 `blocker/` 目录下，描述清楚遇到的问题、需要避免的操作以及正确的方式，方便后续任务避开同样的坑。

## 回复语言

统一用中文回复用户。代码注释、提交信息等写入项目的内容遵循项目现有惯例。

写结论或说明时，不要随意用符号，不要随便用比喻或者意思不直白的表述，用平实、直接的语言说清楚结果。
